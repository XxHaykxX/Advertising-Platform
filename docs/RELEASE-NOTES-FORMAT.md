# Release-notes формат (igovazd.am)

Триггер: пользователь говорит **«Дай мне информацию нового релиза»** (или похоже).
Действие: выдать changelog **на чистом армянском (hy)**, в шаблоне ниже. Коротко, ясно,
по-человечески, без технического жаргона. Сам этот документ — на русском; **армянский только в самом ответе-релизе.**

## Шаблон вывода

```
Թողարկում v{X.Y} — {YYYY} թ. {ամիս-hy}ի {D}

Ի՞նչ նորություն կա, և ի՞նչ շտկվեց igovazd.am-ում.

1. {Կարճ վերնագիր}։ {1–2 простых предложения — что нового / что починили.}
2. ...
```

Правила:
- Первая строка: `Թողարկում v{версия} — {дата на армянском}` (напр. `2026 թ. հուլիսի 17`).
- Вторая строка всегда: `Ի՞нч նорություն կա, և ի՞нч штквец igovazd.am-ум.`
- Нумерованный список; каждый пункт — жирный короткий заголовок + 1–2 простых предложения.
- Только пользовательские изменения (фичи + фиксы). БЕЗ тех-деталей (prisma, tsc, schema, postcss-версии и т.п. — писать «обновление безопасности», не имена пакетов).
- **ВКЛЮЧАТЬ ОБЯЗАТЕЛЬНО (иначе забуду):**
  - **Все Jira/QA-фиксы релиза** (тикеты IA-*, что чинили) — переформулированные для юзера, без номеров тикетов.
  - **Работу по локализации/переводам**, если была (новые языки, инструменты перевода контента) — как пользовательское улучшение «переводы точнее/полнее».
- Чистый армянский шрифт, БЕЗ кириллических гомоглифов (проверять каждую букву — о/е/к/т/р/с/х/а могут пролезть латиницей/кириллицей).
- Версию инкрементить от предыдущей.

## Счётчик версий
- **v1.3** — 2026-07-17 (Phase-1/2: единый hero, упрощённая форма, creator 1:1, актёры-поиск, Registrations→Users, убраны анонимные Applications, фикс logout, translate-устойчивость, телефон +374).
- **v1.4** — 2026-07-17 (уведомления, фильтры каталога, фикс кнопок карточек, report-респонсив, interests бренда, локализация ошибок форм, валюта→footer, security).
- **v1.5** — 2026-07-18 (портфолио-кейсы i18n hy/ru/en + локализация метрик, фикс cookie «запомнить меня», внутренние улучшения стабильности/безопасности — тест-инфра + чистка мёртвого кода).
- **v1.6** — 2026-07-20 (web-push уведомления + live-тостер, избранное брендов, кабинет создателя с сайдбаром+профиль, админ-рассылка push/email, фикс показа постеров, оптимизация картинок WebP, единый 16:10, чистка footer/nav кабинета). Prod HEAD=39c11bd.
- **v1.7** — 2026-07-22 (редизайн About/Mission, фикс перехода из кабинета на страницы проектов, фильтр по полу аудитории, бюджет бренда сохраняется мгновенно вкл. повторные Save, кнопка «назад» на странице проекта, trim пробелов пароля + иконка замка). Prod HEAD=364b8f9.
- **v1.8** — 2026-07-24 (видео на детальной странице проекта embed+MP4; About-блок с описанием/tagline на 3 языках; все картинки 16:9; код проекта скрыт везде; синопсис убран с карточек каталога; глобальный менеджер персон /admin/cast; массовая загрузка медиа + кнопка скачивания; редизайн админки — группированная навигация, сворачиваемый сайдбар, 2-колоночная форма, компактные таблицы cast/tiers, sticky Save; форма edit сохраняет без ухода со страницы + зелёный «Сохранено»/красный «Не сохранено»; курсор-рука на всех кнопках; QA-фиксы: hydration/dnd/потеря данных cast; точнее переводы; безопасность). Prod HEAD=ba648c1. Prod migration: Person + video-поля.
- **v1.9** — 2026-07-26 (редактор переводов интерфейса в админке `/admin/i18n` + роль TRANSLATOR: 855 ключей hy/ru/en, автосейв-черновики, отдельные «Сохранить»/«Опубликовать» с публикацией коммитом в main, цветные метки вместо заливки строк + фильтры по ним, заметки к ключу, Excel-режим таблицы (закреплённые шапка/колонка, компакт, сворачиваемые группы, раскрытие ячейки), хоткеи, копирование между языками, CSV вниз/вверх с превью, undo/redo, бесконечный скролл, валидация пусто/гомоглифы/плейсхолдеры/длина/невидимые; Google-таблица выведена из процесса. Фиксы: колесо мыши во вложенных списках (Lenis глушил wheel), залипающая галочка «сохранено», доступ штатных ролей к медиатеке/проектам, кастомная платформа создателя доезжает в словарь). Prod HEAD=5ed1a46. Prod migration: TRANSLATOR + UiDraft + I18nPublish. Требует `GITHUB_SYNC_TOKEN` в hPanel env.
- Следующий → **v2.0**.
