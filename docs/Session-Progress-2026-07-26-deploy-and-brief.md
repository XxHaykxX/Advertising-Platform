# 26 июля 2026 — деплой волн 1–3 и бриф заявки

Продолжение `docs/Session-Progress-2026-07-26-waves.md` и
`docs/audit-2026-07-26-platform.md`. Здесь: что задеплоено, что найдено в цикле
заявки и что по этому поводу сделано.

---

## 1. Деплой

Владелец выбрал раскатку в два захода — чтобы при поломке было понятно, какая
именно часть виновата. Схема и `package.json` в волне 0 не менялись, так что
первый заход обошёлся без миграций.

| Заход | Коммиты | Миграции | Билд Hostinger | Результат |
|---|---|---|---|---|
| 1 (волна 0) | `4dedb4d`, `c236c3d` | нет | `019f9eaf` completed | прод 200 |
| 2 (волны 1–3) | `dfc188d` … `9043668` (14 коммитов) | 3 шт., применены до push | `019f9eb3` completed | прод 200 |
| 3 (бриф заявки) | `331363e` | `2026-07-26-interest-brief.sql`, применена до push | `019f9ebf` | см. ниже |

Применённые прод-миграции (в этом порядке):

1. `2026-07-26-wave1-audit.sql` — `Project.rejectionReason`, `Project.productionBudgetAmd`
2. `2026-07-26-wave2-interests.sql` — поля `Interest`, таблица `InterestEvent`,
   бэкфилл истории (создал 5 записей по числу существовавших заявок)
3. `2026-07-26-wave3-stats.sql` — `Project.viewCount`
4. `2026-07-26-interest-brief.sql` — бриф на `Interest` и `InterestEvent`

Перед первой миграцией снят дамп прод-базы (17 таблиц). Он лежал во временной
папке сессии и вместе с ней исчезнет — если понадобится точка отката, дамп
нужно снимать заново.

**Проверка, что код и база сошлись вживую:** `POST /api/projects/2/view` → 204,
после чего `Project.viewCount` у проекта 2 стал равен 1. Это подтверждает
именно то, что миграция и новый код видят одну и ту же колонку.

---

## 2. Что нашлось в цикле заявки

Задача от владельца: «проверь логику, когда бренд заполняет форму и отправляет —
какие данные пишет и чего не хватает; и после получения — хватает ли
функционала для ответа».

### Что бренд отправлял (до правок)

Попап на странице проекта собирал ровно три вещи:

| Поле | Обязательное | Куда шло |
|---|---|---|
| Пакет размещения | нет («Не выбран — обсудим») | `Interest.tierId` |
| Сообщение | да, любой длины | `Interest.message` |
| Контакт | нет | `Interest.contact` |

### Чего не хватало получателю

1. **Бюджет и категории бренда не показывались никому.** Бренд заполняет их при
   регистрации (`User.budgetRange`, `User.brandCategories`), но DTO заявки их не
   тянул. Продавец решал по пакету на миллионы, видя имя и почту.
2. **Не было брифа.** По отраслевым руководствам бренд в обращении о размещении
   заявляет минимум: что размещаем, в какие сроки и деньги это или бартер.
   У нас всё это попадало в заявку только если бренд сам догадался написать.
3. **Сообщение без минимальной длины.** «Привет» — валидная заявка, на которую
   продавец обязан ответить.
4. **Бренд не видел собственную заявку.** В его кабинете были статус, пакет и
   ответ продавца — но не текст того, что он отправил. При нескольких заявках
   невозможно вспомнить, где какие условия предложены.

### Дыра в ответе (главное)

`window.prompt(answerPrompt) ?? ""` — в обеих копиях (кабинет создателя и
админка). «Отмена» в промпте давала `null`, `?? ""` превращала её в пустую
строку, **и ответ всё равно отправлялся**. А ответ необратим: меняет статус,
занимает или освобождает слот пакета и шлёт бренду письмо. Случайный клик по
«Принять» откатить было нечем.

---

## 3. Что сделано (коммит `331363e`)

- Попап заявки спрашивает **что размещаем**, **желаемые сроки** и **форму
  оплаты** (Оплата / Бартер / Оплата и бартер). Все три необязательны — бренд,
  который пишет всё прозой, проходит как раньше.
- Сообщение — минимум **20 символов**. Проверяется и в попапе (кнопка
  заблокирована + подсказка), и на сервере (прямой POST не обойдёт).
- Оба входящих раздела (`/account/interests`, `/admin/interests`) показывают
  бриф отдельным блоком над сообщением, плюс **бюджет и категории бренда**.
- Письмо о новой заявке несёт бриф и бюджет — лид оценивается прямо из почты.
- Кабинет бренда показывает, что он отправил.
- Бриф пишется в каждую запись истории (`InterestEvent`), поэтому повторная
  подача с новыми условиями не переписывает то, что предлагалось изначально.
- `dealType` принимает только `CASH` / `BARTER` / `BOTH` — прочее отбрасывается,
  чтобы подделанный POST не положил произвольный текст туда, где UI рисует метку.
- **Починена отмена**: `if (note === null) return;` в обеих копиях.

Не делалось намеренно:

- **Переписка внутри платформы и «запросить уточнение»** — владелец отложил
  (решение C.2 от 26.07). Стандартная тройка в маркетплейсах —
  принять / отклонить / запросить детали, но третья кнопка это уже переговоры.
- **Скрытие контактов бренда до ответа.** Сейчас почта и телефон видны продавцу
  сразу. В бэклоге это лежит как GARM / mutual-reveal и требует решения владельца.

---

### Чем это проверено

E2E (`e2e/member.spec.ts`, 5/5 зелёных против локального dev): бренд
регистрируется, открывает проект из каталога, заполняет бриф и сообщение,
отправляет, находит заявку в своём кабинете; отдельный тест держит минимум в
20 символов (кнопка отправки остаётся заблокированной). Тестовые аккаунты
удаляет `global-teardown`.

Тот же файл до этого падал — но не из-за правок: он кликал кнопку «Проявить
интерес» на странице browse, которую убрали ещё в `dfc188d`. Тест переписан
под поток, который существует.

**Не покрыто автотестом:** ответ продавца (принять / отклонить) — для этого
нужен создатель с опубликованным проектом, то есть прогон через модерацию.
Логика проверена чтением кода; там же найдена и починена дыра с отменой.

## 3a. Правки после первого показа владельцу (коммит `eaeae4b`)

Владелец открыл раздел Applications и упёрся в три вещи.

### Ответить было некому

Отвечать мог только пользователь, чей `id` совпадал с `Project.ownerId`.
На боевой площадке проекты принадлежат создателям, поэтому суперадмин видел
заявку и надпись «The project's owner answers this application» без единой
кнопки. Если создатель молчит — заявка висит вечно, бренд ответа не получает.

Ограничение задумывалось как «сделка — дело продавца» (решение C.2), но на
практике вышло тупиком. Теперь **суперадмин может ответить на любую заявку** —
он владелец площадки. Создатель — по своим проектам. `PUBLISHER` и `MODERATOR`
по-прежнему не решают чужую сделку. Когда отвечает штаб, в историю пишется
`RESPONSE_STAFF`, а не `RESPONSE`, чтобы было видно, кто именно решил.

Замечание на будущее: заявку удобно проверять только на проде — в локальной
базе владельцем проекта стоит `creator@test.com`, а не админ, так что там
кнопок не будет и это нормально.

### Код проекта

`#PP-…` остался в админском списке заявок и в очереди модерации. Убран и там —
это внутренняя нумерация, показывать её незачем (владелец повторял это
несколько раз). В форме проекта скрытое поле осталось: оно передаёт данные, а
не рисует их.

### Телефон

Заявка не несла телефона: продавцу доставались почта и свободный текст в поле
«Контакт», которое бренд мог не заполнять. Теперь:

- телефон **обязателен**, проверяется как международный номер (`+`, код
  страны, 7–14 цифр) и на клиенте, и на сервере;
- пустое поле стартует с `+374` — Армения, домашняя страна площадки;
- номер сохраняется в профиль бренда, поэтому **на следующей заявке поле не
  показывается вовсе** — вместо него строка с уже известным номером
  (требование владельца: не переспрашивать то, что заполнено);
- поле «Контакт» убрано — оно дублировало то, что аккаунт уже знает.

**Длина номера проверяется точно по стране.** Армянский номер — ровно 8 цифр
после `+374` (`+37499105115`); на цифру меньше или больше не проходит, потому
что это опечатка, а опечатка в номере = лид, до которого не дозвониться.
Правила лежат таблицей `PHONE_RULES` в двух копиях (клиент и сервер — вторая
должна держать прямой POST); коды, которых в таблице нет, проверяются мягким
международным диапазоном. Длина зафиксирована тестами `src/lib/phone.test.ts`,
чтобы правку регулярки нельзя было незаметно ослабить.

E2E дополнен: заявка без нормального номера не отправляется (6/6 зелёных),
юнит-тестов стало 164.

## 3b. Второй круг правок — после того, как владелец открыл раздел

### Ответить было некому (вторая часть той же дыры)

Право ответа сверялось по совпадению `user.id` с `Project.ownerId`. На боевой
площадке проекты принадлежат создателям, поэтому суперадмин видел заявку,
надпись «отвечает владелец проекта» и ноль кнопок. Если создатель молчит —
предложение висит вечно. Теперь **суперадмин отвечает на любое предложение**
(он владелец площадки), создатель — по своим, `PUBLISHER`/`MODERATOR` — нет.
Ответ штаба пишется в историю как `RESPONSE_STAFF`, чтобы было видно, кто решил.

Пересматривает решение C.2 «только владелец проекта»: на практике оно оказалось
тупиком.

### Проекты на модерации вели себя как обычные

`PENDING`-проект лежал в `/admin/projects` и выглядел как рядовой неактивный —
отличить «ждёт решения» от «снят с публикации» было нельзя. Теперь Projects
показывает только `APPROVED`; очередь живёт в Moderation (там уже были вкладки
Pending / Rejected / All и счётчики). Над списком — ссылка «N awaiting
moderation», чтобы submission не потерялся. Подпись «N in catalog» считала все
строки подряд — теперь считает опубликованные и отдельно пишет, сколько снято.

### Имя раздела

`Applications` было неверным словом трижды: *application* — это заявление,
которое подают в надежде, что примут, а здесь бренд приходит с деньгами и
называет пакет; слово сталкивалось с двумя другими «заявками» (проекты в
модерации, одобрение аккаунтов); в кабинете создателя «Заявки» читалось как
«мои поданные проекты». Переименовано в **Brand offers / Предложения брендов /
Բրենդների առաջարկներ**, у бренда — «Мои предложения».

### Чего не хватало по мелочи, но заметно

- **Счётчика не было вообще.** Предложение приходило без единого видимого следа.
  Добавлен бейдж в админ-меню и в боковое меню создателя (число уже считалось в
  коде, его просто никто не показывал). Обновляется и при возврате в окно, а не
  только при смене раздела — иначе счётчик стоял на месте ровно тогда, когда за
  ним следят.
- **Клик по названию проекта открывал форму редактирования** — в том числе в
  очереди модерации, где задача судить, а не переписывать. Название открывает
  страницу проекта в новой вкладке, правка — отдельная ссылка.
- **Код `#PP-…`** оставался в админском списке предложений и в очереди
  модерации. Убран (владелец просил несколько раз).
- **Заявка на неодобренный проект.** Проверки не было вообще: прямым POST можно
  было отправить предложение на проект в модерации, отклонённый или снятый с
  каталога. Принимает только опубликованный.

### Телефон бренда

Обязателен, проверяется как международный номер, и длина сверяется по стране:
Армения — ровно 8 цифр после `+374` (`+37499105115`), на цифру меньше или больше
не проходит, потому что это опечатка, а опечатка в номере = лид, до которого не
дозвониться. Правила в `PHONE_RULES`, две копии (клиент + сервер против прямого
POST), границы закреплены `src/lib/phone.test.ts`. Номер сохраняется в профиль,
и если он там уже есть — **попап не спрашивает его снова**, показывает строкой.
Поле «Контакт» убрано: оно переспрашивало то, что аккаунт уже знает.

### Системные окна браузера

Ответ на предложение открывался через `window.prompt()` — серая коробка ОС
посреди панели, одна строка для текста и «Отмена», которая всё равно отправляла
ответ. Заменено на `NoteDialog` в стиле продукта. Тем же диалогом спрашивается
причина отказа в модерации.

Заодно вычищены все остальные: удаление партнёра и кейса, удаление значения
Streaming Source, ошибка дублирования проекта — везде был нативный
`confirm()`/`alert()`. В коде не осталось ни одного. Сам `ConfirmDialog` рисовал
кнопку удаления **синей**, хотя помечен как destructive — теперь красная.

### Права на просмотр непубличного проекта

Было: только модератор и контент-редактор. Переводчик получал 404 на той самой
странице, чьи тексты он проверяет. Теперь — **любой staff** (`isStaff()`), плюс
**сам автор** в любом статусе: он написал проект и после отказа должен видеть,
что отправил. Чужие создатели, бренды и гости — 404, как и раньше.

Проверено вживую по ролям: staff ×4 → 200, владелец → 200, чужой создатель →
404, бренд → 404, гость → 404.

### Push

Push должен доходить до всех типов аккаунтов. На деле **у staff не было даже
кнопки включить** — подписка предлагалась только в кабинете участника. Поэтому
новое предложение создавало запись в ленте и пыталось отправить push аккаунту,
у которого подписки быть не могло. Кнопка добавлена в панель. Отдельно: если
браузер уже заблокировал уведомления, компонент не показывал **ничего** —
аккаунт переставал получать push и не понимал почему; теперь показывается
объяснение.

⚠️ В push уходит только короткий текст («Предложение принято»). Сообщение
продавца видно в кабинете, в ленте и в письме, но не в самом push.

## 4. Грабли этой сессии

- **`npx prisma generate` падает с EPERM**, пока поднят dev-сервер: он держит
  `query_engine-windows.dll.node`. Лечится остановкой процесса на порту 3001.
- **Билд падает на `/opengraph-image`** с `colourspace: parameter space not set`
  (libvips). К коду отношения не имеет и `rm -rf .next` не лечит — проверено,
  падало и на чистом кэше, и с остановленным dev-сервером. Причина в
  параллелизме: `next build` поднимает воркеров по числу ядер (23 на этой
  машине), и sharp/libvips ловит гонку. Лечится теми же переменными, с которыми
  собирает Hostinger:
  ```
  $env:NODE_OPTIONS="--max-old-space-size=4096 --v8-pool-size=2"
  $env:UV_THREADPOOL_SIZE="2"; $env:TOKIO_WORKER_THREADS="2"; npx next build
  ```
  Прод не задет — там ограничение стоит в скрипте сборки.
- **`mysqldump` из образа mysql:8 против сервера Hostinger** требует
  `--column-statistics=0`, иначе валится на `information_schema.COLUMN_STATISTICS`
  и оставляет файл-огрызок в 6 КБ, который выглядит как дамп.
- **Turbopack не принимает junction на `node_modules`**: «Symlink is invalid, it
  points out of the filesystem root». Собрать worktree, подцепив `node_modules`
  ссылкой, нельзя — проще собирать в основном дереве на detached HEAD.

---

## 5. Статус по владельцу

**Сделано владельцем 26.07 (вечер):**

- `.env.hostinger` импортирован в hPanel, приложение перезапущено — значит
  `GITHUB_SYNC_TOKEN` в проде и публикация из `/admin/i18n` работает.
- Аккаунт Мариам заведён в `/admin/users` с ролью `TRANSLATOR`.
- Токен перевыпущен.

Отсюда следует: **редактор переводов запущен в работу**. Мариам правит словарь
(976 ключей) и публикует его коммитом в `main` через GitHub API.

> ⚠️ С этого момента `src/lib/i18n.ts` — файл с двумя авторами. Если Мариам
> нажмёт «Опубликовать», пока у кого-то лежат незапушенные правки этого файла,
> `main` уйдёт вперёд и push отклонится. Перед работой с `i18n.ts` — `git pull`.

**Осталось:**

- Пункт C.7: настоящие телефон, WhatsApp, YouTube, имена основателей. До этого
  витрина остаётся с заглушками. Владелец отложил это на самый последний этап.
- Пункт 2.5 (бриф → договор → оплата → отчёт по кампании) — отложен как
  отдельный проект.
- Мёртвый код: папка `src/app/admin/(panel)/registrations/` — раздела нет в
  меню, самореги не попадают в `PENDING`. Ждёт решения на удаление.
- В web push уходит только короткий текст; сообщение продавца туда не попадает
  (видно в кабинете, в ленте и в письме).
